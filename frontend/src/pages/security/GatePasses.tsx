import React from 'react';
import { Card } from '@/design-system';

const GatePasses: React.FC = () => {
    return (
        <div style={{ padding: '20px' }}>
            <Card>
                <div style={{ padding: '20px' }}>
                    <h2>Gate Passes</h2>
                    <p>Manage security gate passes and approvals.</p>
                </div>
            </Card>
        </div>
    );
};

export default GatePasses;
